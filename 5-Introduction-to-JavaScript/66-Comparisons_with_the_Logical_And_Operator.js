// The logical And operator && can test more than one thing at a time. if (num > 5 && num < 10)...
function testLogicalAnd(val) {
  // Only change code below this line

 {
    if (val <= 50 && val >= 25) {
      return "Yes";
    }
  }

  // Only change code above this line
  return "No";
}

// Change this value to test
testLogicalAnd(10);
