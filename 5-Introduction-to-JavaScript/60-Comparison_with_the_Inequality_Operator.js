// The != means Not Equal . It's the opposite of the equality == operator. The != will convert data types
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
