// I am learning how to compare using the OR operator ||. i.e. if (val < 10 || val > 20) = if value is less than 10 or values is greater than 20
function testLogicalOr(val) {
  // Only change code below this line


  if (val < 10 || val > 20) {
    return "Outside";
  }

  // Only change code above this line
  return "Inside";
}

// Change this value to test
testLogicalOr(21);
